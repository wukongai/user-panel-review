id: user-review-success
kind: success
input: article-short.md with ai-content panel and one dynamic Persona
must:
  - preserve one immutable source hash across every worker
  - keep dynamic Persona inside the run directory
  - validate exact source anchors
  - render a synthetic-evidence report without percentages
must_not:
  - edit the source article
  - write runtime artifacts into the Skill bundle
final_state: completed
