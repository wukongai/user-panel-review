# 使用示例

## 自动选择评审团

> 使用 `$user-review` 评审 `/absolute/path/article.md`。文章属于 AI 内容，目标是在微信公众号帮助普通知识工作者理解一个新工具。先说明你选择了哪些 Persona 和原因，等我确认后再开始。

## 增加本次运行画像

> 现有画像没有覆盖“已经用过很多 AI 工具、但非常担心本地数据丢失的人”。请和我一起补全这个画像，只用于本次评审。

## 保存长期画像

> 刚才的临时画像以后也会用。请先展示保存计划；我确认后再加入长期画像库，并映射到 AI 内容线。

## 指定画像

> 使用 `$user-review`，只让 `ai-01-scroller` 和 `ai-04-power-user` 分别评审这篇文章，保留分歧，不要用多数票抹平少数意见。

